{
  "name": "ddsr-ts-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "DDSR TypeScript Client Framework",
  "license": "EPL-2.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest",
    "test:run": "vitest run",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "typescript": "~5.8.0",
    "vitest": "^4.0.0"
  }
}
